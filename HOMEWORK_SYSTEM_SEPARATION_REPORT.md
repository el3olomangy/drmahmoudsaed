# تقرير: فصل "الواجب" عن "الامتحان" كنظامين مستقلين تمامًا

## الملخص
المنصة كانت أصلًا مبنية على إن "الواجب" هو مجرد امتحان (exam) عليه علامة `is_homework: true`، يعني نفس الـ collection ونفس الكود. الطلب كان نفصل الاتنين خالص، فبقى فيه:

- **نظام امتحانات نضيف** (`exams` collection) — للاختبارات العادية بس، بمدة زمنية (`duration_minutes`) وعداد وقت.
- **نظام واجب مستقل بالكامل** (`homeworks` collection جديدة) — بموعد نهائي إلزامي (`deadline`)، من غير عداد وقت، وحماية كاملة (منع الفتح والتسليم بعد الموعد + إشعار تلقائي للمتأخرين).

## الملفات الجديدة (Backend)
| الملف | الوظيفة |
|---|---|
| `backend/app/schemas/homework.py` | سكيمات الواجب المستقلة (إنشاء، تسليم، نتيجة) |
| `backend/app/models/homework.py` | بناء document الواجب في قاعدة البيانات |
| `backend/app/api/routes/homework.py` | كل راوتس الواجب: إنشاء، عرض، تسليم، نتائج، تصحيح مقالي، تعديل، حذف — مستقلة 100% عن `exams.py` |

## الملفات الجديدة (Frontend)
| الملف | الوظيفة |
|---|---|
| `frontend/app/(student)/dashboard/homework/[id]/page.tsx` | صفحة أخذ الواجب الجديدة (بدون عداد وقت، فيها حالة "انتهى الموعد") |

## الملفات المعدّلة (Backend)
- **`backend/app/schemas/exam.py`** و **`backend/app/models/exam.py`** — اتشالت منهم `is_homework` و`deadline` نهائيًا، رجعوا امتحان نضيف بحت.
- **`backend/app/api/routes/exams.py`** — اتنضّف بالكامل من أي منطق خاص بالواجب.
- **`backend/app/main.py`** — اترجستر راوتر `homework` الجديد.
- **`backend/app/core/scheduler.py`** — بقى بيراقب `homeworks` collection (مش `exams`) لإرسال إشعار "لم تقم بتسليم الواجب".
- **`backend/app/api/routes/progress.py`** — تقدّم الطالب (لولي الأمر والمدرّس) بقى بيجيب بيانات الواجب من `homeworks`/`homework_results` بشكل منفصل تمامًا عن الامتحانات.

## الملفات المعدّلة (Frontend)
- **`frontend/lib/api.ts`** — `homeworkAPI` كائن جديد كامل، و`examsAPI` اتنضّف من `is_homework`/`deadline`.
- **`frontend/app/(admin)/dashboard/admin/assignments/new/page.tsx`** — فورم "واجب جديد" بقى بيستخدم `homeworkAPI.create`، والموعد النهائي بقى **إلزامي دايمًا** (مش اختياري).
- **`frontend/app/(admin)/dashboard/admin/assignments/page.tsx`** و **`[id]/page.tsx`** — قائمة الواجبات وتصحيحها بقت كلها عبر `homeworkAPI`.
- **`frontend/app/(admin)/dashboard/admin/exams/page.tsx`** — نضّفت من فلتر `is_homework` القديم (الاختبارات بس بقت تظهر هنا).
- **`frontend/app/(student)/dashboard/exam/[id]/page.tsx`** — رجعت صفحة امتحان نضيفة بحتة (عداد وقت، بدون أي منطق واجب).
- **`frontend/app/(student)/dashboard/watch/[id]/page.tsx`** — بيجيب الامتحان والواجب من مصدرين مستقلين (`examsAPI` و`homeworkAPI`)، وزرار الواجب بيروح `/dashboard/homework/{id}`.
- **`frontend/app/(student)/dashboard/courses/[id]/page.tsx`** — بيدمج قائمتين مستقلتين (اختبارات + واجبات) في عرض موحّد لكل محاضرة، كل واجب بيروح لصفحته الصح.

## سلوك النظام الجديد
1. **إنشاء واجب**: من "واجب جديد" في لوحة المدرّس — بيطلب موعد نهائي إجباري (تاريخ + وقت)، وأسئلة اختيار من متعدد.
2. **قبل الموعد**: الطالب بيفتح الواجب ويحله عادي، مرة واحدة بس.
3. **بعد الموعد**: أي محاولة فتح (حتى بلينك مباشر) بترجع فورًا "تم انتهاء موعد التسليم" — الأسئلة متتعرضش خالص.
4. **إشعار تلقائي**: كل 5 دقايق، أي واجب فات موعده بيتفحص، وأي طالب من نفس المرحلة (أو مشترك في الكورس لو مدفوع) لسه مسلّمش بياخد إشعار "لم تقم بتسليم الواجب".
5. **الامتحانات العادية** رجعت زي ما كانت بالظبط، من غير أي تأثير من نظام الواجب.

## خطوة أخيرة مهمة قبل التشغيل
لازم تعمل **Restart كامل للباك اند** بعد نسخ الملفات، عشان:
- الراوتر الجديد `homework` يترجستر.
- الـ scheduler الجديد يبدأ يراقب المكان الصح.

## ملاحظة عن قاعدة البيانات
بما إن Firebase Realtime Database من غير Schema ثابت، مفيش أي "migration" مطلوبة — الـ collection الجديدة (`homeworks`, `homework_results`) هتتعمل تلقائيًا أول ما تعمل واجب جديد. أي واجبات قديمة كانت متخزنة في `exams` بعلامة `is_homework: true` من التجربة السابقة **مش هتظهر في النظام الجديد** — تقدر تحذفها يدويًا من Firebase Console لو حابب تنضّف الداتا القديمة، أو تسيبها متجاهلة (مش هتضر حاجة).
