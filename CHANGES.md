# إضافة: عدد الحصص الشهرية + تقرير الشهر المجمّع (حضور + مدفوعات)

الملفات دي بتتحط فوق نفس مساراتها في المشروع (extract over). مفيش أي حاجة اتمسحت — كله إضافات.

## اللي اتعمل

### 1) عدد الحصص جنب كل طالب (بيتصفّر تلقائيًا مع الشهر الجديد)
- الحصة = يوم حضور واحد لكل طالب (زي النظام الحالي).
- العدّ بيتحسب على الشهر الحالي بس، فبيرجع **صفر لوحده** مع بداية أي شهر جديد من غير أي مسح، والداتا القديمة بتفضل محفوظة للتقارير.
- بيظهر دلوقتي في:
  - صفحة طلاب المجموعة (badge "X حصة الشهر" + سطر "حضر X حصة خلال الشهر").
  - نتيجة الاسكان (بعد ما تمسح الطالب بيقولك حضر كام حصة الشهر ده).

### 2) تقرير الشهر لكل مجموعة (جدول واحد)
- تبويب جديد في صفحة التقارير اسمه **"تقرير الشهر"** (بقى أول تبويب).
- جدول واحد لكل طالب فيه: المجموعة + اسم الطالب + عدد الحصص + دفع/مدفعش.
- فوقه ملخّص: إجمالي الحصص، عدد الطلاب، دفعوا، مدفعوش، والمبلغ المحصّل.
- فلترة بالمرحلة/المجموعة، واختيار الشهر (أي شهر قديم كمان).
- زرار **طباعة / PDF** بيفتح نسخة نضيفة للطباعة أو الحفظ PDF.

## الملفات المعدّلة
- `backend/app/api/routes/center.py`
  - `_sessions_count()` — عدّ حصص الطالب في شهر.
  - `_student_out()` — بترجّع `sessions_this_month`.
  - `list_students` و `students-all` — بيحسبوا الحصص بجلب واحد للحضور (أسرع).
  - endpoint جديد: `GET /center/reports/monthly?month=&stage_id=&group_id=`.
  - نتيجة الاسكان بترجّع `sessions_this_month`.
- `frontend/lib/api.ts` — `centerAPI.reportMonthly(...)`.
- `frontend/app/(admin)/dashboard/admin/center/groups/[id]/page.tsx` — عرض عدد الحصص.
- `frontend/app/(admin)/dashboard/admin/center/reports/page.tsx` — تبويب "تقرير الشهر" + الطباعة.
- `frontend/app/(admin)/dashboard/admin/center/scan/page.tsx` — عدد الحصص في نتيجة الاسكان.

## ملاحظات
- مفيش أي تعديل في قاعدة البيانات ولا الفهرسة — الفهارس الموجودة (`date`, `month`, `group_id`, `student_id`) كافية.
- مفيش cron/مهمة تصفير — التصفير حاصل تلقائيًا بحكم إن العدّ بيتفلتر بالشهر.
