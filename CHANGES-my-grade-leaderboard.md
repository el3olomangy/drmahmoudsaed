# إضافة: أوائل مرحلة الطالب في نهاية صفحته الرئيسية

الملفات دي بتتحط فوق نفس مساراتها (extract over).

## اللي اتعمل
- في نهاية صفحة الطالب الرئيسية بيظهر قسم **"أوائل مرحلتك"**: أعلى **5 طلاب** في نفس صف الطالب بس (بنفس استايل نظام التقييم — Level + XP + ميداليات).
- لو الطالب نفسه ضمن الـ 5، صفّه بيتـميّز بإطار ووسمة **"أنت"**.
- لو مش ضمن الـ 5، بيظهر تحت القائمة سطر **"ترتيبك #X من Y"** في مرحلته.
- لو لسه مجمّعش XP، بيظهر سطر لطيف إنه لسه مالوش ترتيب.

## الملفات
- `backend/app/api/routes/gamification.py` → endpoint جديد `GET /gamification/me/rank` بيحسب ترتيب الطالب وسط طلاب مرحلته (التعادل بياخد نفس المركز، محصّن ضد الأخطاء).
- `frontend/lib/api.ts` → `gamificationAPI.getMyRank()`.
- `frontend/components/leaderboard-list.tsx` → دعم `highlightId` لإبراز صف الطالب + وسمة "أنت".
- `frontend/components/my-grade-leaderboard.tsx` → الكومبوننت الجديد (Top 5 + سطر الترتيب).
- `frontend/app/(student)/dashboard/page.tsx` → عرض القسم في آخر الصفحة (بيستخدم `user.grade` و `user.id`).

## ملاحظات
- الترتيب بيتحسب بين طلاب نفس الصف اللي عندهم XP أكبر من صفر.
- طبيعي يظهر فاضي لحد ما الطلاب يجمعوا XP (بدأنا من صفر).
