# تحديثات: أفاتار 3D جديد + إصلاح موبايل صفحة الطلاب + ترتيب المنصة في الأدمن

الملفات دي بتتحط فوق نفس مساراتها (extract over). كلها Frontend — مفيش أي تعديل باك إند.

## 1) أفاتار افتراضي جديد (3D) للبنت والولد
- استبدلت `frontend/public/boy-face.svg` و `frontend/public/girl-face.svg` بأشكال 3D حديثة وأنضف (خلفية متدرّجة + وش وشعر وقميص بظلال ناعمة). ولد للولد، بنت للبنت (بشعر طويل + توكة).
- بما إنها نفس أسماء الملفات، أي مكان بيستخدمها اتحدّث تلقائيًا (البروفايل + مودال تفاصيل الطالب).
- كومبوننت جديد `components/default-avatar.tsx`: بيعرض الشكل حسب النوع (male/female)، ولو النوع مش معروف بيرجع للحروف الأولى من الاسم. بيتستخدم في صفحة الطلاب وقائمة الترتيب.

## 2) إصلاح تنسيق صفحة الطلاب على الموبايل
`app/(admin)/dashboard/admin/students/page.tsx`:
- صف الطالب بقى `flex-wrap`، وأزرار (إيقاف/تفعيل + عرض الملف) بتنزل في سطر تحت بعرض كامل على الموبايل بدل ما تزحّط وتبوّظ التنسيق.
- أضفت سطر صغير للموبايل بيعرض المرحلة وعدد الكورسات (لأن العمود الجانبي بيتخفي على الموبايل).
- الأفاتار بقى بيستخدم الشكل الجديد بدل الحروف.
- على الديسكتوب الشكل زي ما هو بالظبط.

## 3) ترتيب المنصة داخل الأدمن
- صفحة جديدة `app/(admin)/dashboard/admin/leaderboard/page.tsx` بنفس استايل نظام التقييم (رأس متدرّج + Level + XP + ميداليات المراكز)، مع فلترة بالصف وعرض حتى 50 طالب، وملخّص (عدد الطلاب + إجمالي XP المعروض).
- اتضاف لينك "ترتيب الطلاب" في قائمة الأدمن الجانبية (`layout.tsx`) بأيقونة كأس، متاح للمعلم والمساعد.
- كومبوننت قائمة موحّد `components/leaderboard-list.tsx` بيستخدمه الأدمن والصفحة الرئيسية (نفس الشكل، من غير تكرار).
- الصفحة الرئيسية (`leaderboard-section.tsx`) اتظبطت تستخدم نفس القائمة والأفاتار الجديد.

## ملاحظات
- مفيش أي تعديل باك إند — الـ endpoint `GET /gamification/leaderboard` أصلًا بيرجّع الصف والـ Level والـ Title و default_avatar، فالأدمن بيستهلكه بـ limit=50.
- الترتيب لسه هيبان فاضي لحد ما الطلاب يجمعوا XP (لأننا بدأنا من صفر) — طبيعي.
